<div class="card">
  <%= link_to image_tag(recipe.image, class: :card__img ), recipe_path(recipe.id), method: :get %>
  <div class="card__body">
    <%= link_to recipe.title, recipe_path(recipe.id), class: :card__title%>
    <p class="card__summary">
      <%= "レシピジャンル" %>
    </p>
    <%# <%= link_to "by プロトタイプの投稿者名", root_path, class: :card__user %> 
  </div>
</div>
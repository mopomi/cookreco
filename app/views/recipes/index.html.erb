<main class="main">
  <div class="inner">
    <%# ログインしているときは以下を表示する %>
    <% if user_signed_in? %>
      <div class="greeting">
        <%= link_to current_user.name, root_path, class: :greeting__link %>さんのお料理記録
      </div>
    <% end %>
    <%# // ログインしているときは上記を表示する %>
    <div class="card__wrapper">
     <%= render partial: "recipe", collection: @recipes %>
    </div>
  </div>
</main>

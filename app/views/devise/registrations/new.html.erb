<div class="main">
  <div class="inner">
  <div class="form_a">
    <div class="form__wrapper2">
      <h2 class="show">ユーザー新規登録</h2>

        <%# 「モデル名」にはUserモデルであれば@userを渡しましょう。%>
        <%# 「新規登録機能へのパス」は、devise導入後にrails routesを実行してdevise/registrations#createへのパスを確認し、記載してください。 %>
        <%= form_with model: @user, url: user_registration_path, local: true do |f| %>
         <%= render 'shared/error_messages', model: f.object %>

        <div class="field">
          <span class="must">必須</span><%= f.label :email, "メールアドレス" %><br />

          <%= f.email_field :email, autofocus: true, placeholder: "email", autocomplete: "Email", class: :form_user %> 
        </div>

        <div class="field">
          <span class="must">必須</span><%= f.label :password, "パスワード（6文字以上）" %><br />
          <%= f.password_field :password, autocomplete: "new-password", placeholder: "Password", class: :form_user %> 
        </div>

        <div class="field">
          <span class="must">必須</span><%= f.label :password_confirmation, "パスワード再入力" %><br />
          <%= f.password_field :password_confirmation, autocomplete: "new-password",placeholder: "Password（確認）", class: :form_user %>
        </div>

        <div class="field">
          <span class="must">必須</span><%= f.label :name, "ニックネーム" %><br />
          <%= f.text_field :name, placeholder: "Nickname", class: :form_user %>
        </div>


        <div class="actions">
          <%= f.submit "新規登録", class: :form__btn %>
        </div>
      <% end %>
    </div>
  </div>
</div>